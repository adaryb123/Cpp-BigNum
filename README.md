Data structure for numbers so big that they need to be stored in string. Works also for negative numbers. Does not work for decimal numbers.

Supported operators: + , - , * , += , -= , *= , < , <= , == , > , >= ,!=, << , = , +(unary), -(unary)
